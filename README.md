# Графин iWine — OpenAPI 3.0

Тестовый проект RESTful API для "умного" графина iWine.

## Сборка проекта

`yarn install`

`yarn build iWine.yaml --options.theme.colors.primary.main=orange --output ./build/index.html`
